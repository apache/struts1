<config
        host="${host}"
        port="${port}"
        basepath="${basepath}"
        protocol="http"
        summary="true"
        saveresponse="true"
        resultpath="results"
        resultfile="result.txt"
        showhtmlparseroutput="true"/>
